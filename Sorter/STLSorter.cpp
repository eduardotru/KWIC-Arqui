#include <algorithm>

#include "STLSorter.h"

AbstractWordsContainer* STLSorter::sort(AbstractWordsContainer* words) {
    // std::sort(words->container.begin(), words->container.end());
    return words;
}